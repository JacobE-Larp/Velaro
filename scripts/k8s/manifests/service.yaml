apiVersion: v1
kind: Service
metadata:
  name: vilaro
  labels:
    app: vilaro
spec:
  type: ClusterIP
  selector:
    app: vilaro
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
