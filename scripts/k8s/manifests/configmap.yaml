apiVersion: v1
kind: ConfigMap
metadata:
  name: vilaro-config
  labels:
    app: vilaro
data:
  vilaro.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.vilaro/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Vilaro Assistant",
            "workspace": "~/.vilaro/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Vilaro Assistant

    You are a helpful AI assistant running in Kubernetes.
