---
summary: "CLI reference for `velaro clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `velaro clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `velaro clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `velaro clawbot qr` (same behavior as [`velaro qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `velaro clawbot qr` -> `velaro qr`
