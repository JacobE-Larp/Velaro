services:
  vilaro-gateway:
    image: ${VILARO_IMAGE:-vilaro:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      VILARO_GATEWAY_TOKEN: ${VILARO_GATEWAY_TOKEN:-}
      VILARO_ALLOW_INSECURE_PRIVATE_WS: ${VILARO_ALLOW_INSECURE_PRIVATE_WS:-}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      TZ: ${VILARO_TZ:-UTC}
    volumes:
      - ${VILARO_CONFIG_DIR}:/home/node/.vilaro
      - ${VILARO_WORKSPACE_DIR}:/home/node/.vilaro/workspace
      ## Uncomment the lines below to enable sandbox isolation
      ## (agents.defaults.sandbox). Requires Docker CLI in the image
      ## (build with --build-arg VILARO_INSTALL_DOCKER_CLI=1) or use
      ## docker-setup.sh with VILARO_SANDBOX=1 for automated setup.
      ## Set DOCKER_GID to the host's docker group GID (run: stat -c '%g' /var/run/docker.sock).
      # - /var/run/docker.sock:/var/run/docker.sock
    # group_add:
    #   - "${DOCKER_GID:-999}"
    ports:
      - "${VILARO_GATEWAY_PORT:-18789}:18789"
      - "${VILARO_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${VILARO_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  vilaro-cli:
    image: ${VILARO_IMAGE:-vilaro:local}
    network_mode: "service:vilaro-gateway"
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      HOME: /home/node
      TERM: xterm-256color
      VILARO_GATEWAY_TOKEN: ${VILARO_GATEWAY_TOKEN:-}
      VILARO_ALLOW_INSECURE_PRIVATE_WS: ${VILARO_ALLOW_INSECURE_PRIVATE_WS:-}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      TZ: ${VILARO_TZ:-UTC}
    volumes:
      - ${VILARO_CONFIG_DIR}:/home/node/.vilaro
      - ${VILARO_WORKSPACE_DIR}:/home/node/.vilaro/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - vilaro-gateway
